# New Project Example

apiVersion: 1

# Unique name of this dataset which will be used in this project to
# refer to this dataset

name: "disaster_messages"
type: source

schema:
  primary_keys: []

materializations:
  - type: table
    target: layer
    table_name: "disaster_messages"

    
name: "disaster_messages_categories"
type: source

schema:
  primary_keys: []

materializations:
  - type: table
    target: layer
    table_name: "disaster_messages_categories"
